{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}

{% block header %} {% endblock %}

    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Email</th>
                <th scope="col">...</th>
                {# <th scope="col">Handle</th> #}
            </tr>
        </thead>
        <tbody>
            <tr>
                <th scope="row">1</th>
                    <td>{{ app.user.email }}</td>
                    <td>
                        <a href="{{ path('users_profil_modifier') }}" type="button" class="btn btn-success">Edit</a>
                        <button type="button" class="btn btn-danger">Delete</button>
                    </td>
                    {# <td>@mdo</td> #}
            </tr>
            <tr>
                <th scope="row">2</th>
                    <td>Jacob</td>
                    {# <td>Thornton</td> #}
                    {# <td>@fat</td> #}
            </tr>
            {# <tr>
                <th scope="row">3</th>
                    <td colspan="2">Larry the Bird</td>
                    <td>@twitter</td>
                </tr> #}
        </tbody>
    </table>

    <h2>Mes commandes</h2>
   
    {# {% for commande in app.user.commandes %}

    {% endfor %} #}
{% endblock %}
