{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}

{% block header %} {% endblock %}
    
    <h1>Modifier mes coordonnées</h1>
    
    {{ form_start(form) }}
        {{ form_row(registrationform.name, {'attr': {'class': 'input-color'}}) }}
        {{ form_row(registrationform.firstname, {'attr': {'class': 'input-color'}}) }}
        {{ form_row(registrationform.tel, {'attr': {'class': 'input-color'}}) }}
        {{ form_row(registrationform.email, {'attr': {'class': 'input-color'}}) }}
        {{ form_row(registrationform.password, {
            label: 'Password',
            'attr': {
                 'class': 'input-color'
            }
        }) }}


        <button type="submit" class="btn">Register</button>
    {{ form_end(form) }}
{% endblock %}
